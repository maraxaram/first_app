<%= form_for(@qmask) do |f| %>
  <% if @qmask.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@qmask.errors.count, "error") %> prohibited this qmask from being saved:</h2>

      <ul>
      <% @qmask.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :input %><br />
    <%= f.text_field :input %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
